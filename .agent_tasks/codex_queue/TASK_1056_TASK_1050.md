TASK_NAME: TASK_1050_step5_pretraining_config

BACKGROUND:
STEP 5 requires a pretrained checkpoint (valid_trained_eval=True) to replace the random-init
artifacts from STEP 4. The training infrastructure already exists:
- scripts/02_train_text_smoke.py (entrypoint — DO NOT MODIFY)
- src/frcgw/training/train_text.py (run_smoke_train + SmokeResult — DO NOT MODIFY)
- configs/train_text.yaml (base config for reference)

Stage 1 plan: max_steps=200, epochs=3 (smoke)
Stage 2 plan: max_steps=500, epochs=5 (extended, conditional)
Dataset: data/frcgw_text/v0_3/ (train split via configs/dataset_v0_3.yaml if exists,
  otherwise data_config: configs/data_collection_text.yaml as fallback)

GOAL:
1. Create configs/train_text_v0_3.yaml (Stage 1 config: max_steps=200, epochs=3, seed=42)
2. Create configs/train_text_v0_3_stage2.yaml (Stage 2 config: max_steps=500, epochs=5, seed=42)
3. ADD monitoring functions to src/frcgw/training/monitoring.py (FILE ALREADY EXISTS — DO NOT RECREATE):
   The file already has PublicTraceLogger class. ADD the following new functions/classes at the bottom:
   - check_losses_finite(losses: list[float]) -> bool
   - check_grad_norm(norm: float, threshold: float = 100.0) -> bool (True if OK)
   - check_f_t_variance(values: list[float], threshold: float = 1e-4) -> str:
     returns "OK", "DEGENERATE_LOW_VARIANCE", or "INSUFFICIENT_DATA"
     ("INSUFFICIENT_DATA" when len(values) < 3)
   - TrainingMonitorState dataclass: step, loss, grad_norm, f_t_variance_status, alerts: list[str]
   - build_monitor_csv_row(state: TrainingMonitorState) -> dict (for CSV logging)

   IMPORTANT: F_t is passed as None to compute_total_loss in the training loop (l_falsification=0.0).
   Therefore check_f_t_variance SHOULD NOT depend on actual training F_t output.
   Instead, the Stage 2 gate uses held-out loss decrease, NOT F_t variance.
   The check_f_t_variance function is for future use (STEP 6) but must exist now for T1 tests.
4. Create tests/test_step5_pretraining_checkpoint.py with 5 tests:
   - test_v0_3_config_schema(): verify max_steps=200, epochs=3, seed=42, data_config present
   - test_stage2_config_schema(): verify max_steps=500, epochs=5, seed=42
   - test_monitoring_losses_finite(): check_losses_finite([1.0, 0.5, 0.2]) == True; [float('nan')] == False
   - test_monitoring_grad_norm(): check_grad_norm(50.0) == True; check_grad_norm(101.0) == False
   - test_monitoring_f_t_variance(): variance >=1e-4 → "OK"; <1e-6 → "DEGENERATE_LOW_VARIANCE";
     insufficient samples → "INSUFFICIENT_DATA"

FILES_ALLOWED:
- configs/train_text_v0_3.yaml
- configs/train_text_v0_3_stage2.yaml
- src/frcgw/training/monitoring.py
- tests/test_step5_pretraining_checkpoint.py

FILES_FORBIDDEN:
- outputs/**
- outputs/checkpoints/**
- data/**
- paper_context_ref/**
- src/frcgw/schemas/visibility.py
- .claude/**
- scripts/run_codex_task.ps1
- scripts/02_train_text_smoke.py
- src/frcgw/training/train_text.py

REQUIRED_IMPLEMENTATION:
1. configs/train_text_v0_3.yaml:
   - version: 1, phase: CC-P3-STEP5
   - seed: 42
   - batch_size: 8
   - max_steps: 200
   - max_epochs: 3
   - lr: 0.001
   - weight_decay: 0.0001
   - model_config: "configs/model_text.yaml"
   - data_config: "configs/data_collection_text.yaml"
   - split: "train"
   - objective_weights: (same as train_text.yaml)
   - optimizer: "adam"
   - manifest_dir: "outputs/runs/p3_train_v0_3_stage1"
   - gate_thresholds: (same as train_text.yaml)
   - compute_budget: "step5_stage1: max_epochs=3, max_steps=200, batch_size=8"
   - forbidden_fields: (same list as train_text.yaml)
   - notes: "STEP 5 Stage 1 pretraining smoke. Extends base smoke to 200 steps."

2. configs/train_text_v0_3_stage2.yaml:
   - Same as v0_3 but max_steps: 500, max_epochs: 5
   - manifest_dir: "outputs/runs/p3_train_v0_3_stage2"
   - compute_budget: "step5_stage2: max_epochs=5, max_steps=500, batch_size=8"
   - notes: "STEP 5 Stage 2 pretraining smoke. Conditional on Stage 1 pass."

3. src/frcgw/training/monitoring.py: as specified above

4. tests/test_step5_pretraining_checkpoint.py: 5 tests as specified

REQUIRED_TESTS:
pytest tests/test_step5_pretraining_checkpoint.py -q
Expected: 5 passed

ACCEPTANCE_CRITERIA:
- 5 tests pass
- configs/train_text_v0_3.yaml has max_steps=200, max_epochs=3, seed=42
- configs/train_text_v0_3_stage2.yaml has max_steps=500, max_epochs=5, seed=42
- monitoring.py: check_losses_finite, check_grad_norm, check_f_t_variance, TrainingMonitorState, build_monitor_csv_row all present
- NO modifications to scripts/02_train_text_smoke.py or src/frcgw/training/train_text.py
- NO writes to outputs/ or data/

COMMIT_MESSAGE:
feat(step5/task1): v0_3 pretraining configs + monitoring hooks

STOP_CONDITION:
Stop if: (1) cannot write to configs/ or src/frcgw/training/ (permissions issue),
(2) train_text.py import fails, (3) test count != 5
