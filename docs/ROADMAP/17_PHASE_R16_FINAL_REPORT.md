# Phase R16 — Final Report and Reproducibility Package

## Goal
LaTeX assembly, final figures, reproducibility package for submission.

## Deliverables
1. LaTeX source in docs/paper/ (to be created in R14)
2. Final figures (PDF/SVG) generated from experiment outputs
3. Reproducibility package: config/ + seeds/ + data_hashes/ + model_checkpoints/
4. Code release: src/fglc/ + scripts/fglc/ + tests/ (public repo)

## Reproducibility Requirements
- Every result traceable to: config file + seed + dataset hash + model checkpoint SHA256
- `scripts/fglc/11_generate_reports.py` reads only from artifact files (no manual numbers)
- All figures generated by scripts (not manual drawing tools)

## Gate Criteria
- [ ] LaTeX compiles without errors
- [ ] All figures traceable to run artifacts
- [ ] Reproducibility package ZIP created
- [ ] Final NLL/return/AUROC numbers verified against stored artifacts
- [ ] Supplementary appendix included in submission PDF
