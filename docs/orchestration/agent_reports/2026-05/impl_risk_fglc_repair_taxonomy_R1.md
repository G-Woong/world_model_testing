# T3 Implementation-Risk-Critic Report

## TASK: TASK_2026_05_23_FGLC_REPAIR_TAXONOMY
## Date: 2026-05-23
## Verdict: PASS (RISK-01 LOW — non-blocking)

---

## Gatekeeper Pre-Check Results

### 1. Scope violation (forbidden paths): PASS

FILES_ALLOWED에 포함된 4개 경로:
- `src/fglc/repair/__init__.py` — 신규 생성, 금지 경로 아님
- `src/fglc/repair/taxonomy.py` — 신규 생성, 금지 경로 아님
- `tests/test_fglc_repair_taxonomy.py` — 신규 생성, 금지 경로 아님
- `.agent_tasks/codex_done/TASK_2026_05_23_FGLC_REPAIR_TAXONOMY_RESULT.md` — RESULT.md 표준 경로

FILES_FORBIDDEN: `"** (everything else)"` + `"특히: src/fglc/schemas/, .claude/, CLAUDE.md, docs/, scripts/, configs/, outputs/"` — 모든 금지 경로 명시적 열거.

`src/fglc/schemas/`가 FILES_ALLOWED에 없고 FILES_FORBIDDEN에 명시적으로 포함되어 있어 visibility.py 보호 충족.

### 2. Test coverage: PASS

REQUIRED_TESTS 명령: `.venv\Scripts\pytest.exe -q tests/test_fglc_repair_taxonomy.py`
6개 테스트 그룹이 REQUIRED_IMPLEMENTATION §5에 명시됨:
1. Enum well-formedness: 20 members, SCREAMING_SNAKE_CASE, str subclass
2. CAUSE_METADATA completeness: 모든 Enum 멤버에 metadata 존재
3. source_md_refs nonempty + 경로 존재 확인 (RISK-01 — 수정 필요)
4. applicable_phases subset of canonical phase set
5. DETECTION_THRESHOLDS values are int|float|None (callable 금지)
6. applicable_phases_for() 반환 정확성

### 3. Scope creep: PASS

GOAL에서 명시적으로 3개 파일만 생성. STOP_CONDITION에 diagnose.py / candidates.py / compare.py 금지 명시. Out-of-scope 항목 모두 커버됨.

### 4. Scientific contract: PASS

ACCEPTANCE_CRITERIA에 명시: `No import from src/fglc/schemas/`. DETECTION_THRESHOLDS에 callable 금지. FailureCauseId 20개 멤버가 SSoT 1:1 일치 확인.

### 5. Implementation risk: PASS

- `@dataclass(frozen=True)` 명시
- `source_md_refs: tuple[str, ...]` — tuple 명시
- `applicable_phases: tuple[str, ...]` — tuple 명시
- `frozenset[FailureCauseId]` 반환 타입 명시
- `No external deps beyond stdlib` 명시

---

## Risk Items

**RISK-01**: `IMPLEMENTATION_BUG_SUSPECTED`의 source_md_refs 처리 불일치
- **상세**: `docs/idea/FGLC_FAILURE_TAXONOMY.md` #20은 source MD = `—` (없음). TASK spec 라인 48은 `>=1 element each`로 기술하고 테스트 그룹 3도 `source_md_refs nonempty` 검증.
- **영향**: Codex가 SSoT 우선하면 `source_md_refs=()` 빈 tuple → 테스트 그룹 3 FAIL 가능.
- **Severity**: LOW
- **Fix**: REQUIRED_IMPLEMENTATION §4 라인 48 + 테스트 그룹 3에 `IMPLEMENTATION_BUG_SUSPECTED` 예외 조건 추가. → 메인 세션이 TASK 파일 수정 후 Codex enqueue 권장.

---

## Recommendation

**PASS — proceed to Codex** (RISK-01 수정 후).

| # | 조건 | 상태 |
|---|---|---|
| 1 | verify mode 종료 코드 0 | Codex 실행 전 예측 불가 — TASK 구조상 HIGH 가능성 |
| 2 | diff clean | FILES_ALLOWED 4개, STOP_CONDITION 명시 — PASS 예상 |
| 3 | 금지 경로 미수정 | FILES_FORBIDDEN 명시 — PASS |
| 4 | RESULT.md 존재 | FILES_ALLOWED 포함 — PASS 예상 |
| 5 | REQUIRED_TESTS 통과 | RISK-01 해소 시 all green |

---

*Generated by: implementation-risk-critic agent (T3 compact mode)*
*Session: 2026-05-23, Step 5 FGLC Repair Taxonomy*
